version: 2.1

# Executors define environment where jobs will be run.
executors:
    snippets:
        docker:
            - image: cnerg/snippets
            
jobs:
    # CI initializing job.
    init:
        executor:
            name: snippets
        steps:
            - checkout
            - run: ls -la
    # Template.
    # test_script_x:
    #     executor:
    #         name: snippets
    #     steps:
    #         - checkout
    #         - run: 
    #             name: Test script X.
    #             command: 
                
workflows:
    test:
        jobs:
            - init
    # Template.
    # test_scripts:
    #     jobs:
    #         - test_script_x
    #         - test_script_y:
    #             requires:
    #                 - test_script_x
